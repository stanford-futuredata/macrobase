taskName: testTasks
targetAttributes: [sex, marital_status, age]
targetLowMetrics: []
targetHighMetrics: [income]

categoricalAttributes : [ hubway_id, bike_nr, subsc_type, gender, zip_code]
numericalAttributes: [duration]
numInterval: 20
minFrequentSubSpaceRatio: 0.5
maxSparseSubSpaceRatio: 0.03

dbUrl: postgres
baseQuery: SELECT * FROM hubway_trips;

minSupport: 0.01
minInlierRatio: 1

useZScore: true
zScore: 10.0

usePercentile: false
targetPercentile: 0.01

logging:
  level: INFO

  loggers:
    "macrobase": TRACE