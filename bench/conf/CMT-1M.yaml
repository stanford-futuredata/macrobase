macrobase:
  macrobase.loader.db.baseQuery: SELECT * FROM mapmatch_history H, sf_datasets D WHERE H.dataset_id = D.id AND userid IS NOT NULL LIMIT 1000000;
  macrobase.loader.targetHighMetrics: [data_count_minutes]
  macrobase.loader.targetLowMetrics: []
  macrobase.loader.attributes: [userid]
  macrobase.loader.auxiliaryAttributes: [battery_delay_from_drive_end_seconds]
  kernelBandwidth: NORMAL_SCALE
  macrobase.analysis.kde.bandwidthAlgorithm: NORMAL_SCALE
  macrobase.analysis.binnedKde.numBins: 100000
  macrobase.loader.loaderType: POSTGRES_LOADER
post_run:
  - plot_estimator: "--savefig --yscale log --xlabel contb_receipt_amt --ylabel count --ymin 0.1"
  - plot_estimator: "--savefig --yscale log --xlabel contb_receipt_amt --ylabel count --ymin 0.1 --x-limits 50 400"
