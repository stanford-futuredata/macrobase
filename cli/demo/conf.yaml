pipeline: "BatchPipeline"

inputFile: "demo/sample.csv"

summarizer: "apriori"

metric: "usage"
percentile: 1.0
includeLo: true
includeHi: true

attributes:
  - "location"
  - "version"
minSupport: 0.2
minRiskRatio: 10.0
